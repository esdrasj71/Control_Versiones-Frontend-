import { FiltradocustomersPipe } from './filtradocustomers.pipe';

describe('FiltradocustomersPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltradocustomersPipe();
    expect(pipe).toBeTruthy();
  });
});
