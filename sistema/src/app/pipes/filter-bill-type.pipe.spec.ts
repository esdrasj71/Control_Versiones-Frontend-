import { FilterBillTypePipe } from './filter-bill-type.pipe';

describe('FilterBillTypePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterBillTypePipe();
    expect(pipe).toBeTruthy();
  });
});
