import { FiltradouserPipe } from './filtradouser.pipe';

describe('FiltradouserPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltradouserPipe();
    expect(pipe).toBeTruthy();
  });
});
