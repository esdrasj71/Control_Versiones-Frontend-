import { FiltradoEmpleadosPipe } from './filtrado-empleados.pipe';

describe('FiltradoEmpleadosPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltradoEmpleadosPipe();
    expect(pipe).toBeTruthy();
  });
});
