import { FiltradoinventoryPipe } from './filtradoinventory.pipe';

describe('FiltradoinventoryPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltradoinventoryPipe();
    expect(pipe).toBeTruthy();
  });
});
