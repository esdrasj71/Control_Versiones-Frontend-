import { FilterExpendituresPipe } from './filter-expenditures.pipe';

describe('FilterExpendituresPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterExpendituresPipe();
    expect(pipe).toBeTruthy();
  });
});
