import { FiltradopresentationPipe } from './filtradopresentation.pipe';

describe('FiltradopresentationPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltradopresentationPipe();
    expect(pipe).toBeTruthy();
  });
});
