import { FilterBankPipe } from './filter-bank.pipe';

describe('FilterBankPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterBankPipe();
    expect(pipe).toBeTruthy();
  });
});
