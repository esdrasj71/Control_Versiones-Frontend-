import { FilterProductCatPipe } from './filter-product-cat.pipe';

describe('FilterProductCatPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterProductCatPipe();
    expect(pipe).toBeTruthy();
  });
});
