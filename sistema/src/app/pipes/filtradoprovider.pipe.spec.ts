import { FiltradoproviderPipe } from './filtradoprovider.pipe';

describe('FiltradoproviderPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltradoproviderPipe();
    expect(pipe).toBeTruthy();
  });
});
