import { FilterEmployeePosPipe } from './filter-employee-pos.pipe';

describe('FilterEmployeePosPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterEmployeePosPipe();
    expect(pipe).toBeTruthy();
  });
});
